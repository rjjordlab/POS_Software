@model IEnumerable<POS_aplicacao.Models.LoginAcesso>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EmpregadoID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Senha)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EmpregadoID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Senha)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AcessoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AcessoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AcessoID })
        </td>
    </tr>
}

</table>
